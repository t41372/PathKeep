# Long-running agent architecture (2026) [agent-architecture]

## SOTA SUMMARY

By mid-2026 the field has converged on a clear shape, and it is good news for a Rust desktop app: the agent itself is a thin while-loop (call model -> if tool calls, execute and append results -> repeat until a final answer), and essentially all real engineering lives in the systems wrapped around that loop. Anthropic, OpenAI, and the analysis of Claude Code all agree the loop is trivial; "context engineering" (deciding which tokens occupy the window) and "harness/loop engineering" (durability, permissions, recovery) are the work. The dominant context strategy is a tiered compaction cascade applied cheapest-first (drop -> selectively clear tool results -> collapse -> LLM summarize), triggered by a token-headroom buffer (Claude Code reserves ~13k tokens; the Claude Agent SDK defaults compaction at ~100k), combined with structured external note-taking (a NOTES.md / todo file outside the window) and just-in-time retrieval (carry lightweight IDs/queries, fetch on demand). Anthropic reports context editing alone gives +29% and editing+memory-tool +39% on long-horizon tasks, with up to 84% token reduction on web-search-style work. Sub-agents are now the canonical scaling lever: each runs in an isolated context window and returns only a 1-2k token summary to the parent, which both prevents context pollution and parallelizes work. For durability the consensus is that SDK-level "session resume" is not enough for long runs; production agents need a durable-execution layer (journaled steps, idempotent tool calls, replay that skips completed boundaries, long-pause for human-in-the-loop) — but LLM non-determinism forces you to journal-and-replay model/tool outputs rather than re-invoke. On framework choice, the loudest 2026 advice is roll-your-own loop first and adopt only the primitives you actually need; the heavy Python/TS SDKs (Claude Agent SDK, OpenAI Agents SDK, LangGraph) ship as Python/TS and would be an awkward, fat dependency for a Rust/Tauri app, whereas a Rust-native ecosystem (Rig 7.7k stars, plus AutoAgents, OpenFang, Radkit) and the official MCP Rust SDK (rmcp) now exist. The landscape is fast-moving and most Rust agent crates are pre-1.0, so maturity/supply-chain caveats matter.

## TOP PICKS: Roll-your-own Rust agent loop (provider-agnostic) + rmcp for tools, Tiered compaction cascade + sub-agent isolation + structured note-taking (context engineering bundle), Durable-execution layer built on PathKeep's existing lease-based job queue + sidecar SQLite, Rig as optional Rust provider/tool plumbing under the custom loop

## CANDIDATES

- **Roll-your-own Rust agent loop (provider-agnostic) + rmcp for tools** [production | rmcp: open source (MIT/Apache class, mod… | rmcp ~3.5k stars (Anthropic/modelcontextprotocol org, official); the loop patter…]: Best fit for PathKeep. Model is remote/user-configured so you do NOT need any in-process model runtime; the loop just needs an HTTP client and the four banned-dependency rules are trivially satisfied (pure-Rust deps, no Homebrew/pkg-config, no build.rs downloads). Gives full control over evidence-citation, PME (Preview->Manual->Execute), consent gating, and off-main-thread execution. Reuses PathKeep's existing lease-…
- **Tiered compaction cascade (context engineering technique)** [production | N/A (technique) | Core of Claude Code (4-tier pipeline) and Claude Agent SDK (default compaction ~…]: Directly applicable. PathKeep should implement at least snip + tool-result clearing + LLM summarize. Reserve a fixed headroom buffer (Claude Code uses ~13k tokens). The summarize step is one extra model call, fine for a remote model. Keeps long browser-history investigations from blowing the window while preserving evidence pointers.
- **Sub-agent context isolation (summary-return pattern)** [production | N/A (pattern) | Anthropic multi-agent research system, Claude Agent SDK native subagents, Claude…]: Useful but apply selectively on the 8GB/4-core envelope: each concurrent sub-agent is another in-flight remote call and another context to track. Parallel fan-out across 14.4M-row searches is attractive, but cap concurrency (e.g. 2-3) and lean on PathKeep's deterministic search to pre-filter so sub-agents summarize small result sets, not raw scans. Map sub-agents onto existing job-queue leases for free durability.
- **Durable-execution layer (journaled steps + idempotent tool calls + long-pause)** [production | N/A for your own impl; DBOS Transact is … | Temporal/Restate/DBOS are mature for servers; the LLM-specific patterns (idempot…]: Strong fit conceptually, and PathKeep should NOT pull in Temporal/Restate (they require external server processes — incompatible with local-first desktop). Build the journal on the existing job queue + a sidecar SQLite (NOT the canonical encrypted DB; agent traces are rebuildable derived state). Key rules to copy: journal model/tool outputs and replay them (don't re-call), idempotency keys per tool call, and a long-p…
- **Structured note-taking + todo planning (NOTES.md / TodoWrite)** [production | N/A (technique) | Claude Code TodoWrite/plan mode; Spring AI ported TodoWrite; widely adopted 2026]: Cheap, high-value, transparency-friendly. The todo list and notes file double as the user-facing PME 'plan preview' and as durable state to resume after a crash. Store in the rebuildable sidecar, render live in the chat UI to satisfy PathKeep's trust/transparency requirement. Off-main-thread; updates stream to the UI.
- **Cross-session memory: tiered store (Letta/MemGPT model) vs Anthropic memory tool** [beta | Letta: open core; memory-tool pattern: N… | Letta/MemGPT well known; Anthropic memory tool in public beta 2026; Mem0/Zep/Gra…]: Implement the lightweight memory-tool PATTERN, not a heavyweight memory platform. Memory must be (a) optional/consent-gated like all AI, (b) stored in the rebuildable sidecar not the canonical encrypted DB, (c) user-inspectable/deletable for data sovereignty. Reuse PathKeep's existing embedding/semantic-search stack (Qwen3-Embedding 0.6B default) for memory recall instead of adding a new memory dependency.
- **Rig (0xPlaygrounds) — Rust LLM/agent framework** [production | MIT | ~7.7k GitHub stars (0xPlaygrounds org) — above PathKeep's ~6k supply-chain thres…]: The strongest off-the-shelf Rust option and it clears the trust gate (>6k stars, active, pure Rust, permissive license). Could provide provider abstraction + tool trait so PathKeep doesn't hand-write per-provider JSON shapes. But it does NOT give durability, compaction cascade, subagents, or memory — you'd still build the harness. Evaluate as 'provider/tool plumbing under your own loop' rather than a full agent runti…
- **AutoAgents (liquidos-ai) — Rust multi-agent framework** [beta | Dual MIT / Apache-2.0 | ~685 GitHub stars — BELOW the 6k trust threshold; would require a written risk a…]: Interesting for its actor model + WASM tool sandbox, but pre-1.0 and under the supply-chain bar (685 stars), so it would need a risk assessment to adopt. No durable/resumable execution. Better as a reference for patterns (Ractor supervision for subagents, WASM tool isolation) than a direct dependency for a shipping local-first product.
- **OpenFang (RightNow-AI) — Rust 'Agent Operating System'** [beta | Dual MIT / Apache-2.0 | ~17.9k GitHub stars (well above threshold), 2.3k forks, 1,767+ tests]: Clears the stars gate but architecturally MISFIT for PathKeep: it's a daemon/server with its own scheduler, channels (Telegram/Discord/etc.), and OS-like surface — far too much, and embedding it as a library is not its model. Running a separate 32MB daemon contradicts local-first single-app simplicity and the 8GB envelope. Mine it for ideas (WASM dual-metering, three-tier memory, SQLite+vector persistence) but do not…
- **Radkit (agents-sh) — Rust agent SDK with A2A** [experimental | MIT | Low (small/young project, agents-sh org)]: Conceptually well-aligned (provider-agnostic, typed tools, optional SQLite persistence, control-first, WASM-portable which matters for any front-end agent code), but v0.0.x and low adoption put it below the trust gate. Watch it; treat as a design reference for clean Rust agent typing rather than a shippable dependency today.
- **Anthropic Claude Agent SDK (claude-agent-sdk)** [production | Anthropic open-source SDK license (MIT-c… | Official Anthropic; claude-agent-sdk v0.1.71, anthropic-sdk-python v0.97.0]: Best-in-class patterns, wrong shape for PathKeep. It is Python/TS-only and Claude-specific, which violates both the provider-agnostic requirement (no hardcoded model/provider) and the lean native-dependency posture (a 300MB Python/Node wheel + bundled CLI in a Tauri app). Use it as the authoritative SOURCE of patterns (loop stages, compaction defaults, subagent isolation, hooks) and reimplement natively in Rust. Do n…
- **OpenAI Agents SDK (+ Temporal/Restate durability)** [production | MIT-class (OpenAI open SDK) | Official OpenAI; widely adopted]: Same mismatch as the Anthropic SDK: Python/TS, provider-leaning, and its durability story leans on external orchestrators (Temporal/Restate) that are inappropriate for a local-first desktop app. Valuable as a reference for the snapshot/rehydrate durability model and the to_json()/from_json() resumable RunState pattern, which PathKeep can mirror over its own job queue.
- **LangGraph (durable state-machine agents)** [production | MIT | Very widely adopted (LangChain ecosystem)]: Architecturally the best durability reference (checkpoint-after-step, SQLite backend, replay-safety contract, idempotency keys) and notably it can checkpoint to SQLite — directly analogous to PathKeep's sidecar-SQLite plan. But Python/TS, so use only as a design template. The key transferable lessons: checkpoint after each loop step, journal+replay non-deterministic LLM/tool outputs, idempotency keys per tool call, r…
- **MCP (Model Context Protocol) as the tool layer + rmcp Rust SDK** [production | Open source (modelcontextprotocol org) | rmcp ~3.5k stars (modelcontextprotocol org, official), 543 forks, 78 releases]: Recommended as PathKeep's internal tool-interface boundary. Defining the agent's tools (history search, semantic search, evidence fetch, note/todo write) as MCP tools gives a clean schema-validated contract, future-proofs for user-added external MCP servers, and the MCP Tasks primitive maps naturally onto PathKeep's long-running job queue. rmcp is below 6k stars but is the OFFICIAL Anthropic/modelcontextprotocol SDK …

## EMERGING/OVERLOOKED: Loop engineering / the 'Ralph' pattern: drive the agent from an outer scheduler (cron/while-loop) that re-prompts against a spec and lets the agent pick one task per iteration, starting fresh each time — a cheap durability+context-reset str… | MCP Tasks primitive (long-running tool invocations) and MCP Apps (tool-returned interactive UI) — Tasks could back PathKeep's long agent runs natively; Apps could let a tool render a history chart/evidence table inline (agnt.one 2026, rmcp … | Claude Code's microcompact innovation: defer clearing tool results until cache ranges are safe to avoid invalidating tens of thousands of cached tokens — a concrete cost optimization most reimplementations miss (Claude Code architecture ana… | Diminishing-returns / 'spinning wheels' detection: auto-stop if the model produces <500 tokens for 3+ consecutive turns — a cheap guard against runaway long-running agents on a constrained machine (Claude Code analysis 2026-03). | WASM tool sandboxing with fuel/epoch metering (AutoAgents, OpenFang) — relevant if PathKeep ever lets the agent run user/extension-provided tools; pairs well with Tauri's security model. | TodoEvolve (arXiv 2602.07839, 2026-02): meta-planning that synthesizes task-specific planning structures rather than a fixed todo template — research-stage but points at where planning is heading for open-ended requests. | DBOS embedded durable-execution model (library co-located with your DB, ~7 LOC, SQLite-capable) is the right philosophical template for local-first durability — but there is NO Rust SDK as of 2026, so it can only be a design reference, not … | Resumable SSE streams via Last-Event-ID for reconnecting the UI to an in-flight agent after the window/app reloads — directly relevant to never freezing the Tauri main thread while a long run continues.

## OPEN Qs: Provider abstraction build-vs-adopt: hand-roll per-provider JSON (full control, zero dep) vs depend on Rig (7.7k stars, clears trust gate) for 20+ providers — needs a decision given the team previously declined rig.rs internally. Does adopt… | Where exactly do agent traces/journal/memory live? Confirmed they must be rebuildable sidecar state (NOT the canonical encrypted SQLite), but: one sidecar DB vs per-session files? Encrypted at rest like the canonical DB, or plaintext derive… | Checkpoint granularity on the existing job queue: coarse (whole-loop boundary, simple, replays the run) vs per-step (model call + each tool call journaled, finer replay, more storage). What's acceptable storage/overhead for a multi-hour run… | Token counting without a local model: rely on tiktoken-rs (provider-specific, may drift from a user-configured arbitrary provider) vs provider token-count endpoints (network round-trips) — needed to drive the compaction trigger reliably acr… | Sub-agent concurrency cap on 4-core/8GB: how many parallel sub-agents/remote calls before the UI or the box degrades? Each is network-bound (remote model) so CPU may not be the limit, but memory of multiple contexts + rate limits are. Needs… | How does the agent loop interact with the 14.4M-row store safely? Tools must enforce bounded/paginated queries (never full scans) and return small evidence sets; what tool-result size budget keeps the context lean while still citing real ro… | Human-in-the-loop / PME pause semantics: a long-pause that releases the worker is ideal, but how is an indefinitely-pending approval surfaced and resumed in a single-user desktop app (no server to wake it)? Likely a job-queue state + UI not… | MCP-internally-or-not: is the rmcp dependency worth it for IN-APP tools, or only adopt MCP at the boundary for EXTERNAL user-added tools and keep internal tools as plain Rust traits? Tradeoff is schema/UX consistency vs an extra dependency …

## DEEP DIVES

### Roll-your-own Rust agent loop (provider-agnostic) + rmcp for tools — VERDICT: recommend

- mem@scale: The agent never holds the 14.4M-row archive in memory — it issues bounded tool calls and sees only result pages, so agent-side RAM is independent of archive size. Dominant memory is the in-flight message transcript: a long agent run with compaction stays well under the model context window (design target <50% of advertised max for reliable reasoning; compact at ~80%). A realistic transcript of a few hundred KB to low single-digit MB of text lives in RAM during a run and is checkpointed to the sidecar each turn. At a typical 128-256k-token window, the transcript is bounded to roughly 0.5-1 MB of UTF-8 even before compaction. rmcp/tokio runtime overhead is a few MB. This is trivially within th…
- cpu: The loop is I/O-bound on the (typically remote, user-configured) LLM; CPU cost of the orchestration is negligible vs the 4-core budget. End-to-end latency is dominated by model round-trips: a multi-tool agent answer is N sequential turns x (network + model latency), typically seconds-to-tens-of-seconds, gated by the model not by PathKeep. Tool execution (history search) runs against SQLite FTS5/bm25 and returns bounded result sets in single-digit-to-low-tens of ms even at 14.4M rows (lexical sea…
- native/supply: rmcp: language Rust (pure); official modelcontextprotocol org; Apache-2.0; v1.7.0 (2026-05-13); ~3.5k GitHub stars (BELOW the 6k numeric gate) BUT qualifies via the 'reputable maintainer/org' clause — it is THE official MCP SDK with ~2.57M downloads/month, used by ~1,800 crates, and is already a PathKeep dependency. Native deps: NONE — core is tokio/serde/serde_json/futures/async-trait/tracing; reqwest/hyper/oauth2/jsonwebtoken only behind optional HTTP-transport features, all pure Rust. No buil…
- incr: The agent loop is stateless derived state: the canonical SQLCipher archive is untouched by the agent. Conversation transcripts, agent traces, and NOTES live in a rebuildable sidecar (consistent with PathKeep's storage-truth model). Resumability: persist the transcript per turn and checkpoint after every tool call into the existing ai_queue rows (le…
- pros: The loop itself is ~30 lines and Anthropic explicitly recommends building agents directly on the API rather than adopting a framework you don't understand (anthropic.com/engineering/building-effective-agents). Roll-your-own gives full contr… | Zero new heavy dependencies for the loop. rmcp is pure Rust (no sys crates; tokio/serde/futures core, reqwest/hyper only behind optional HTTP-transport features), Apache-2.0, ~2.57M downloads/month, official modelcontextprotocol org -> pass… | Perfect fit for the durability requirement: the agent transcript + per-tool checkpoint pattern maps 1:1 onto the existing lease/heartbeat/stop_requested AI queue (ai_queue.rs) and cooperative-checkpoint runtime (job_runtime.rs). No new dura… | Provider-agnostic by construction, satisfying 'no model IDs in copy' and 'user-configured remote model'. The loop only needs a trait with one method (chat-with-tools, streaming). Provider quirks are isolated behind that trait, swappable per…
- cons: You own the provider-normalization layer (request shaping, tool-call encoding, SSE streaming differences across OpenAI/Anthropic/Ollama). This is the genuine cost and the part most likely to break when providers change APIs. Mitigation: dep… | You also own context engineering (compaction, summarization, budget/turn caps, loop detection). Frameworks bundle some of this; rolling your own means writing and TESTING it. The 2026 consensus is the loop is trivial but 'the real engineeri… | rmcp 1.x is young and pre-2.0; minor breaking changes have appeared across 1.x. Pinning and a thin internal wrapper are advisable. (Historical note: the original 4t145/rmcp repo was archived 2025-04 and folded into the official SDK; the pub… | No durable execution / multi-agent orchestration is provided out of the box by rmcp (it's a protocol SDK, not an agent framework) — you must wire it to the existing queue yourself.
- integration: 1) Keep rmcp (bump 1.3.0 -> pin a current 1.7.x behind a thin internal wrapper to absorb 1.x churn). 2) Add a provider abstraction in vault-core: `trait LlmClient` (chat-with-tools + streaming), with per-provider impls; either hand-rolled over the existing reqwest (rustls-tls) or delegating to rig-core/genai for normalization. Provider chosen from user AiProviderConfig (already modeled). 3) Implement the loop (~a few hundred lines incl. hardening) in vault-worker: max-turns cap, wall-clock timeout, token/cost budget, loop detection, 'tools return error strings', compaction trigger at ~80% wind…

### Context-engineering bundle (tiered compaction cascade + sub-agent isolation + structured note-taking) for the PathKeep long-running history agent — VERDICT: recommend (as a harness/prompt-level pattern; reject the RL-trained "context-folding" variant)

- mem@scale: The context-engineering bundle's own footprint at 14.4M docs is effectively flat and tiny: it stores plan/todo/progress notes (kilobytes) plus assistant traces (a few KB/turn) in the derived intelligence sidecar — it does NOT hold the archive in RAM and does not scale with row count. So on 8GB it is a non-issue. The 14.4M-scale memory pressure is a SEPARATE concern (the vector index the agent searches), and it confirms why this bundle is the right place to invest while vectors stay carefully bounded: at Qwen3-Embedding-0.6B's native 1024 dims, INT8 storage is 1024 bytes/vector -> 14.4M x 1KB = ~14.4 GB just for raw vectors, which BLOWS the 8GB budget (HF Qwen/Qwen3-Embedding-0.6B; community …
- cpu: The bundle's compute lives in the model calls (remote, or a local small model the user opted into), not in PathKeep's process, so the 4-core/no-GPU CPU is barely touched by the context machinery itself. Concretely: (1) Note I/O is kilobyte-scale file writes/reads + a few rows in derived/history-intelligence.sqlite per step — sub-millisecond, off the UI thread on the existing worker. (2) Compaction/folding/sub-agent summaries are extra LLM round-trips: latency is dominated by the remote provider …
- native/supply: No new native (C/C++) dependencies are introduced by the bundle itself. rig-core: pure Rust, MIT license, 7.7k GitHub stars (above the ~6k trust gate), maintained by 0xPlaygrounds with named production users (St. Jude, Neon, Coral), v0.39.0 released 2026-06-19, WASM-compatible core => no native build step beyond the standard cargo compile. rmcp (only if we adopt MCP transport): official modelcontextprotocol org (reputable-org clause satisfies the trust gate even at ~3.5k stars), pure Rust + toki…
- incr: Notes and traces are append-mostly and cheap to update: each agent step writes/edits a small note file and inserts a trace row; folding/compaction overwrites a summary field. There is no index to rebuild — this is all rebuildable derived state per PathKeep's storage law (it lives in derived/history-intelligence.sqlite and sidecar note files, never …
- pros: Prompt/harness-level: every piece (tool-result clearing, fold-on-subtask-done, full compaction, sub-agent summaries, file notes) works against an off-the-shelf remote or local model with zero fine-tuning. This is exactly the regime PathKeep… | Anthropic reports concrete, replicable gains for the no-training version: +39% task success (memory+context-editing) and 84% token reduction on a 100-turn eval (Claude blog 2025-09-29) — directly relevant to an agent that must run long over… | Sub-agent isolation maps cleanly onto PathKeep's tool surface: a 'history search' sub-agent can run dozens of FTS5/bm25 + semantic queries against the derived DBs, burn a big internal budget, and hand back a 1-2K-token evidence digest — kee… | Structured note-taking is the durability bridge: writing plan/todo/progress + cited evidence rows to files lines up with PathKeep's already-existing lease-based job queue (job_runtime.rs) and the derived/history-intelligence.sqlite assistan…
- cons: The headline 'context-folding' efficiency (10x smaller active context at parity) is an RL-trained result on a specific 36B model; PathKeep cannot reproduce it with a remote/user model. Selling the bundle on that number would be misleading —… | Compaction is lossy by design and a documented reliability hazard: mis-summarizing one constraint/row-id propagates as ground truth, and goal-drift after compaction (agent declares done or asks to re-clarify) is the 'most insidious' failure… | Real compaction bugs are widespread in 2026 agents: silent empty-summary fallbacks with no error logged, and compaction itself overflowing the window (prompt > 200K) — both filed against shipping agents (GitHub openclaw #2656, #20760, 2026)… | Sub-agent isolation adds orchestration overhead and latency: each sub-agent is extra round-trips to a remote model, and the orchestrator only sees a 1-2K summary, so it can lose the detail needed to course-correct. For a single-user desktop…
- integration: Wire it into the existing worker + intelligence sidecar, no new infra class. 1) Orchestrator loop: a new vault-worker module implementing plan -> act (call a typed tool) -> observe -> update-notes, driven by rig-core for the provider/streaming layer. 2) Tool surface: expose PathKeep's deterministic strengths as the agent's tools first — FTS5/bm25 lexical search, trigram/typo recall, deterministic insights (open-loop/revisit/query-ladder), row-level evidence fetch — so the agent is fully useful with NO embeddings (local-first/AI-optional). Add a 'semantic_search' tool only when an optional vect…

### Durable-execution layer on PathKeep's lease-based job queue + sidecar SQLite (roll-your-own, extending the existing queue) — VERDICT: recommend

- mem@scale: The durable-execution layer's footprint is decoupled from the 14.4M-doc archive — the key scale insight. Journal storage scales with NUMBER OF AGENT STEPS, not history rows. A long agent run of ~200 steps with ~2-8KB of serialized result/trace each is ~0.4-1.6MB on disk per run; in RAM only the active run's working context is held, bounded by the model's context window, not the archive. Recovery scans are O(running jobs), not O(14.4M). So at full 14.4M-archive scale the agent-durability layer adds single-digit MB of sidecar disk per retained run and a few MB of process RAM for the active run — trivially inside the 8GB budget. Contrast: a framework like Temporal/Restate would add a co-residen…
- cpu: Journaling overhead is negligible on the 4-core/8GB/no-GPU target: SQLite WAL does 10K-50K sustained writes/sec (NVMe) and 80K-150K inserts/sec at low compute (2025-2026 benchmarks). An agent turn writes ~1-5 journal rows; that is sub-millisecond and never the bottleneck. The real latency in an agent run is the remote LLM call (typically a user-configured remote model: hundreds of ms to seconds per turn) — durable journaling adds <0.1% overhead to that. Claim/heartbeat/recovery queries are singl…
- native/supply: The recommended core adds ZERO new native dependencies: built on rusqlite (bundled-sqlcipher-vendored-openssl), already vetted and in the build — C backend vendored and compiled during cargo build, no Homebrew/apt/pkg-config, fully reproducible. Reject set and their supply-chain cost: Temporal (separate Go/Java cluster + DB — disqualified), Restate (separate Rust server binary embedding RocksDB — adds a co-resident daemon + native RocksDB; restatedev/sdk-rust exists but the engine is a separate …
- incr: Insert: each agent step appends one journal row in the same transaction that marks the step complete (atomic, ACID via SQLite). Update: job lifecycle transitions reuse the existing CAS UPDATEs (claim/heartbeat/succeed/fail/cancel) in ai_queue.rs. Delete/reindex: the entire agent journal is rebuildable derived state — it can be dropped and the canon…
- pros: Zero new heavyweight infrastructure: no second long-running daemon, no RocksDB, no cluster. Temporal needs 'a dedicated cluster — history service, matching service, worker processes' with 10-50ms/activity dispatch (byteiota 2026-05); Restat… | 2026 consensus explicitly endorses this exact pattern: DBOS positions itself as 'Temporal-level durability with lower operational complexity' as an in-process library, with guidance to 'ship durable execution with DBOS and come back to Temp… | The primitives are already implemented, tested, and reviewed in this repo (lease, heartbeat, CAS claim, stale recovery, retry/backoff, cooperative stop). Adding step journaling is an incremental extension, not a new subsystem — far lower co… | SQLite WAL throughput is wildly over-provisioned for agent journaling: 10K-50K sustained writes/sec on NVMe, 80K-150K inserts/sec at low compute (multiple 2025-2026 benchmarks). An agent writes a handful of rows per LLM turn; journaling cos…
- cons: Roll-your-own means PathKeep owns the hard correctness edges itself: the classic crash-after-side-effect-before-journal-write window (Morling 2025-11: 'if the system crashes after a step executed but before the result is recorded, that step… | Single-node only by construction. SQLite durable execution has a 'single-instance deployment ceiling' (byteiota 2026). A non-issue for a desktop app (one process, one user) but the design must never assume distributed workers — actually a s… | No free framework goodies: no built-in workflow-versioning, no parallel-step fan-out engine, no management UI, no exponential-backoff-by-default — all gaps Morling explicitly flags. PathKeep must hand-build the few it needs (it already has … | The existing `ai_jobs` queue lives in the CANONICAL SQLite, but agent journals/traces are rebuildable derived state that the storage model says belongs in a sidecar. Need a deliberate decision to put the new `agent_runs`/`agent_steps` table…
- integration: 1) Keep the queue, move the durable boundary down a level. Today: one `ai_jobs` row per assistant question, executed atomically by complete_claimed_assistant_job. New: that job becomes an AGENT RUN; introduce a sidecar SQLite (next to semantic_index_dir, NOT canonical) with `agent_runs(run_id, job_id, status, model_provider_id, created_at, schema_version, ...)` and `agent_steps(run_id, step_index, kind {plan|tool_call|tool_result|llm_turn|final}, idempotency_key, request_json, result_json, citations_json, completed_at)` plus UNIQUE(run_id, step_index). 2) Reuse the queue verbatim for durabilit…

## CRITIC FINAL REC: Roll your own thin Rust agent loop — the research is right on this — but reframe the priorities. The loop is the easy 10%; the 90% is (1) the vector-search TOOL the agent calls, which the research treats as solved and is not, and (2) replay-safe durability and context discipline.

Architecture for PathKeep:

- LOOP: hand-written tokio while-loop in the Rust backend. Use a NARROW provider abstraction — prefer the small single-purpose genai crate (with a written risk assessment, 806 stars) OR hand-roll 3-4 provider JSON shapes; treat Rig as optional/at-threshold, not a dependency to lean on. Stream tokens to the UI, support hard cancellation, and enforce a per-run step + token/cost budget. Detect tool-calling capability and degrade to deterministic search when the user's model can't tool-call.
- TOOLS as plain Rust traits (search, fetch-evidence, aggregate, write-note, update-todo, bounded sub-search), 6-10 of them, static cached schema. Adopt rmcp ONLY at the EXTERNAL-MCP boundary, behind consent — not for calling your own in-process functions. Be honest that rmcp is 0.x / ~2.7k stars and clears the gate on 'reputable org', not stars.
- CONTEXT: implement selective recency-pruning (drop whole stale tool-result pairs) + lightweight summarization as the PRIMARY strategy (per arXiv 2606.10209), reserving expensive LLM auto-compact as a last resort. Keep working tokens well under ~32k (Context Rot). Carry row IDs + counts, fetch evidence just-in-time, NEVER inline large result sets. Persist a citation table (row ID -> evidence) in the sidecar so citations survive compaction — directly serving the transparency requirement.
- DURABILITY: extend the existing lease-based job queue over a sidecar SQLite (rebuildable derived state, NOT the canonical encrypted DB). Do NOT adopt Temporal/Restate/DBOS/the iopsystems 'durable' crate (all server/Postgres/WASM). Contract from day one: journal each model + tool output BEFORE observing it; on resume replay journaled bytes, never re-invoke the model (avoids re-charging the user and divergence); idempotency keys per tool call; a long-pause state for PME approvals.
- SUB-AGENTS: support but cap concurrency at ~2, run at lower priority, feed them PRE-FILTERED deterministic results, and forbid them from triggering bulk embedding — otherwise they break the UI-fluidity hard requirement on 4 cores.
- MEMORY: implement the lightweight Anthropic memory-tool PATTERN over the sidecar, reusing the existing embedding stack for recall, consent-gated and user-deletable.

Hard dependency this area inherits: the agent's semantic-search tool CANNOT be in-RAM HNSW (14.4M x 1024-dim fp32 HNSW ~61GB; even int8 512-dim ~7.4GB at the 8GB ceiling). It must be disk-resident ANN with MRL-512 + int8 quantization (e.g. usearch with its vendored-C-backend crate, which PathKeep's rules explicitly ALLOW, since the archived pure-Rust DiskANN crate fails the trust gate). The agent must stay fully useful in deterministic FTS5/bm25/trigram mode while the multi-hour-to-multi-day (≈10-40h at ~100-400 docs/s) one-shot embedding backfill runs — and that backfill IS the headline use case for the durable job queue. Bottom line: ship the simple loop, spend the engineering budget on replay-safe durability, recency-pruned context with a persistent citation table, and a disk-quantized vector tool — and explicitly cap sub-agent concurrency to protect the main thread.

## CRITIC scale_failure: The agent loop itself never breaks at 14.4M rows — it is a thin while-loop making remote HTTP calls. What breaks is the TOOLING the agent calls, and the research underweights this by treating the vector layer as a solved sibling concern. Concrete failure modes on 4-core/8GB/no-GPU:

(1) VECTOR INDEX RAM BLOWUP — the real wall. Qwen3-Embedding-0.6B is 1024-dim. In-memory HNSW at fp32: 14.4M x (1024x4 + ~40 neighbors x 4) = 14.4M x ~4256B = ~61GB. Even at 768-dim it is ~46GB; the search result's 768-dim/40-neighbor figure of ~3232B/vector x 14.4M = ~45GB. Either way, 6-8x over the 8GB budget. Raw fp32 vectors alone (14.4M x 1024 x 4) = ~59GB on disk. So pure-in-RAM HNSW is categorically impossible and the agent CANNOT assume a fast in-memory semantic-search tool exists. The viable path is disk-resident ANN (DiskANN/Vamana/SPANN) with int8/scalar-quantized vectors held in RAM as a guide: in…
