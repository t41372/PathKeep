# Memory-constrained ANN at 14.4M on 8GB [vector-store-memory]

## SOTA SUMMARY

As of mid-2026 the memory-constrained ANN problem at this scale is solved not by one index but by a quantize-in-RAM + rerank-from-disk pattern. The hard wall is raw size: 14.4M x 1024-dim (Qwen3-Embedding 0.6B default) f32 = ~59 GB, far over 8 GB; even int8 scalar (~14.7 GB) does not fit. The two viable shapes are (a) graph-based disk-resident indexes (DiskANN/Vamana: compressed codes in RAM for routing, full vectors mmap'd on SSD, fetched only to rerank) and (b) IVF + aggressive quantization (cluster, store 1-bit codes, rerank with full vectors from disk). The dominant 2025-2026 quantization advance is RaBitQ / Extended-RaBitQ (SIGMOD 2025), an asymptotically-optimal 1-bit-per-dimension quantizer with a theoretical error bound that is a drop-in replacement for binary/scalar/PQ; it compresses ~32x while holding recall >94% at 768d/10M and supports B-bit modes (4/5/7-bit ~= 90/95/99% recall before rerank). At 1-bit, 14.4M x 1024d codes are only ~1.84 GB, which fits in RAM with headroom, while full f32 vectors live in an mmap'd on-disk sidecar (~59 GB) read only during rerank. Binary quantization is reliable specifically because Qwen3-0.6B emits >=1024 dims (Qdrant's guidance: BQ needs >=1024d), and Matryoshka truncation (1024->512/256) can further cut RAM. The cleanest supply-chain fit for an embeddable Rust/Tauri app is usearch (vendored single-file C++, on-disk view, i8/b1/f16 quant, CRUD) for the index plus a hand-rolled RaBitQ-or-binary + disk-rerank layer; pure-Rust DiskANN crates exist but are low-trust (<=10 stars), faiss-next is a non-starter (needs system/Homebrew faiss, 3 stars), and Intel SVS / VectorChord / LanceDB are not embeddable here (Intel-only/closed, or Postgres-bound) though LanceDB and VectorChord are the best technique references for the IVF+RaBitQ+disk-rerank pattern.

## TOP PICKS: usearch (Rust crate) as the index/serving engine, RaBitQ / Extended-RaBitQ ported to Rust as the quantizer, Binary-quantization oversample-and-rescore pattern (disk rerank from mmap'd full-vector sidecar), hnswlib-rs / hnsw_rs as the pure-Rust plan-B if usearch is rejected

## CANDIDATES

- **usearch (unum-cloud)** [production | Apache-2.0 | ~4.2k GitHub stars (just under the 6k auto-trust bar -> needs a short risk note,…]: Strongest embeddable fit. b1 binary @ 14.4M x 1024d = ~1.84 GB codes (fits 8GB); view() mmaps index from disk so RAM stays bounded; i8 path (~14.7 GB) is too big for RAM-resident but fine on-disk. HNSW graph adds ~1.5-2 GB at M=16. Caveat: usearch's on-disk 'view' is still an HNSW graph that must be traversed; for 14.4M, pair RAM-resident 1-bit codes with a separate mmap'd full-vector file for rerank rather than rely…
- **RaBitQ / Extended-RaBitQ (VectorDB-NTU)** [beta | Apache-2.0 (reference library). | ~227 stars on RaBitQ-Library; influential via adoption in LanceDB, Elasticsearch…]: The quantizer PathKeep should adopt (port to Rust, ~hundreds of lines). At 1-bit: ~1.84 GB codes for 14.4M x 1024d; >94% recall@10M/768d reported, and with full-vector rerank from the on-disk sidecar recall recovers to ~0.98+. Beats PQ for PathKeep: no k-means codebook training cost, theoretical error bound, faster distance estimation, trivial incremental insert (per-vector encode, no global codebook). Pair with IVF …
- **Binary Quantization + oversample-and-rescore (Qdrant pattern)** [production | Apache-2.0 (Qdrant). | Qdrant ~20k+ stars; pattern is industry-standard.]: Defines PathKeep's serving recipe and key constraint: BQ needs >=1024 dims to retain enough information -> Qwen3-0.6B's 1024d is exactly on the safe edge (do NOT MRL-truncate below 1024 with plain BQ; RaBitQ tolerates lower dims better). Reported ~0.98 recall@100 at 4x oversampling for 1536d, up to 40x speed from 1-bit SIMD. Concrete: 2x-4x oversampling x final-K candidates fetched as f32 from the mmap'd sidecar for …
- **DiskANN / Vamana (disk-resident graph)** [experimental | MIT / MIT-Apache across the ports. | Reference DiskANN concept is dominant (Milvus, Azure, Timescale); Rust ports ~10…]: Architecturally ideal for the 8GB/14.4M envelope (RAM ~ compressed codes, SSD ~ full vectors); reference DiskANN reportedly does billion-scale in 64GB -> implying well under 8GB for 14.4M of compressed codes. But adopting a 10-star Rust port as core infra is a supply-chain risk; better to take the _pattern_ (RAM codes + SSD rerank) and build it on usearch/RaBitQ. Reconsider only if a Rust DiskANN crosses the trust ba…
- **faiss IVF / IVF-PQ / OPQ (via faiss-next Rust)** [production | MIT (binding); faiss is MIT. | faiss ~30k+ stars; the Rust binding ~3 stars.]: REJECT for embedding into Tauri: the only maintained Rust path needs system-installed faiss, violating supply-chain rules and reproducible-build principle. Keep faiss only as the conceptual baseline for IVF-PQ math (PQ ~128 B/vec -> ~1.84 GB at 14.4M; build time 2-5x IVF-Flat). RaBitQ supersedes PQ here anyway.
- **SPFresh / SPANN (incremental disk IVF)** [research | MIT (SPTAG). | SPTAG ~5k stars; SPFresh is a 2023 SOSP research artifact.]: Directly relevant as the answer to incremental updates at scale: its ~10 GB RAM / 2-core billion-scale result implies our 14.4M is comfortably in-envelope conceptually, and its in-place-update design models how to grow PathKeep's archive without full rebuilds. But not embeddable (no Rust crate, large C++ system). Use as design reference for the update/rebalance strategy of the sidecar.
- **Intel SVS (Scalable Vector Search) -- LVQ / LeanVec** [production | Mixed; core compression closed-source. | Intel-backed; now also surfaced via faiss CPU wiki.]: REJECT: Intel-only hardware + closed-source compression breaks cross-platform local-first (PathKeep targets generic 4-core desktops incl. ARM/AMD) and supply-chain transparency. No Rust path. Notable only as evidence that CPU-side quantization (LVQ/LeanVec) is a leading 2026 direction -> RaBitQ is the open, portable equivalent.
- **hnswlib-rs / hnsw_rs (jean-pierreBoth, pure Rust)** [beta | Apache-2.0 OR MIT. | ~242 stars (below trust bar -> risk note, but it is pure-Rust and inspectable; s…]: Viable pure-Rust fallback if usearch is rejected. BUT it has NO built-in quantization, so the in-RAM graph would index full or externally-quantized vectors -> you must layer RaBitQ/binary codes + disk rerank yourself. mmap covers only dumped vector data, not the graph, so graph RAM (~1.5-2 GB at M=16/14.4M) is resident. Lower trust (242 stars) than usearch but zero native-dep risk. Good plan-B for a fully-Rust stack.
- **Matryoshka (MRL) dimension truncation** [production | Model: Qwen3-Embedding (Apache-2.0). | Qwen3-Embedding family widely adopted; MRL standard since 2024.]: Orthogonal lever to stack with quantization. Caution: combining MRL (e.g. ->512) with plain binary quantization is risky because BQ wants >=1024d; RaBitQ degrades more gracefully. Best use: offer MRL-512 + RaBitQ as a 'low-RAM' tier (~0.92 GB codes at 14.4M) and full-1024 + RaBitQ as the default. Keep model/dim choices behind config; no model IDs in product copy.
- **VectorChord (tensorchord) / LanceDB IVF_RQ** [production | Apache-2.0 (LanceDB); license check need… | VectorChord production at 3B+ vectors; LanceDB trending 2025-2026.]: Best real-world proof that IVF+RaBitQ+disk-rerank hits PathKeep-class envelopes (100M/32GB -> 14.4M well under 8GB for codes). Use as architecture/parameter reference. LanceDB is the only one even theoretically embeddable, but adopting it means a second storage engine and a different sidecar shape -- evaluate only if hand-rolling RaBitQ proves too costly.

## EMERGING/OVERLOOKED: RaBitQ/Extended-RaBitQ (SIGMOD 2025) is the single most important 2025-2026 shift: an asymptotically-optimal 1-bit quantizer with a theoretical error bound that removes PQ's codebook-training cost and makes incremental insert trivial -- ado… | SAQ (arXiv 2509.12086, 2025) 'code adjustment + dimension segmentation' pushes quantization further than RaBitQ -- worth tracking but very new/unproven for an embeddable Rust stack. | TurboQuant (arXiv 2504.19874, 2025) online near-optimal quantization -- competitor to RaBitQ; Milvus published a TurboQuant-vs-RaBitQ cost comparison in 2026. | 2026 disk-graph evaluation (arXiv 2603.01779) finds existing disk layouts waste IO (<=15% utilization) and that smaller pages + dimension-aware layout win -- informs how to lay out the on-disk full-vector sidecar for rerank. | The 'rebuildable derived sidecar' framing is a strength: because the vector index is NOT canonical, PathKeep can periodically rebuild instead of supporting hard in-place delete, sidestepping the hardest part of disk-ANN (SPFresh-style in-pl… | Binary-quant's >=1024-dim requirement makes Qwen3-0.6B's 1024d the safe default and argues AGAINST MRL-truncating below 1024 unless using RaBitQ. | int8/i8 quantization (usearch supports it) is a middle tier -- but at 14.4M x 1024d it is ~14.7 GB, too large to keep RAM-resident on 8GB, so 1-bit is the right RAM tier and i8/f32 belong on disk for rerank.

## OPEN Qs: Does usearch's b1 (1-bit) path expose RaBitQ-style corrective scalars + rerank, or only raw Hamming HNSW? If only Hamming, PathKeep must add its own RaBitQ encode + disk-rerank layer on top of usearch (or hnsw_rs) rather than relying on use… | Measured end-to-end recall@10 and query latency for RaBitQ-1bit + f32 disk rerank specifically on Qwen3-0.6B 1024d embeddings of browser-history text at 14.4M -- published numbers are 768d; need an in-envelope benchmark. | Build/encode time for the full 14.4M sidecar on a 4-core 3GHz CPU (one-shot bulk import): RaBitQ encode is cheap per-vector, but HNSW/IVF graph construction at 14.4M is the real cost -- needs profiling to confirm it can be chunked/backgroun… | Random-read amplification of rerank: fetching k x oversampling full f32 vectors (4 KB each at 1024d) from the mmap'd 59 GB sidecar per query -- quantify IOPS/latency on commodity SATA/NVMe SSD vs a slow disk; decide oversampling factor (2x-… | Incremental-update policy: lazy-delete + tombstones + periodic full rebuild of the derived sidecar vs SPFresh-style rebalancing -- what rebuild cadence keeps recall stable as the archive grows toward 14.4M without a multi-minute UI stall? | usearch at ~4.2k stars is under the 6k auto-trust bar: confirm whether Unum's maintainer reputation clears the 'reputable maintainer/org' exception or whether a written risk assessment is required. | Is a pure-Rust stack (hnsw_rs + hand-rolled RaBitQ + custom disk-rerank) preferable to usearch on the supply-chain/transparency axis, accepting more in-house code and a 242-star dependency, to avoid any bundled C++? | Should the sidecar be a single mmap'd flat file (simplest, matches 'not in canonical SQLite') or a small dedicated SQLite/columnar store for codes+offsets -- and how to key vectors back to canonical visit rows for the evidence/citation requ…

## DEEP DIVES

### usearch (Rust crate) as ANN index/serving engine for PathKeep — VERDICT: conditional — viable ONLY in binary-quantized in-RAM HNSW + exact-rerank mode; reject the disk-mmap view() mode at 14.4M; supply-chain trust-gate not met (4.2k stars < 6k), needs a written risk assessment

- mem@scale: At 14.4M × 1024-dim. Raw f32 = 14.4M×1024×4 = ~59 GB (impossible). HNSW graph overhead with uint40 IDs at M=16 (~32 edges/node × 5 B) ≈ 14.4M × ~160 B ≈ ~2.3 GB regardless of vector precision. Options: (a) f32 in-RAM = 59 GB vectors + 2.3 GB graph — impossible. (b) i8 scalar in-RAM = 14.7 GB + 2.3 GB ≈ 17 GB — still exceeds 8 GB. (c) i8 at 512-dim MRL = 7.4 GB + 2.3 GB ≈ 9.7 GB — still over budget on an 8 GB box with OS+app overhead. (d) BINARY b1 at 1024-dim = 14.4M×128 B = 1.84 GB vectors + 2.3 GB graph ≈ 4.1 GB — FITS with headroom. (e) binary at 512-dim MRL = ~0.92 GB + ~2.3 GB graph ≈ 3.2 GB — comfortable, leaves RAM for embedding model + app + page cache. CONCLUSION: the only RAM-resid…
- cpu: Build: usearch reports ~9.6-10.7x faster indexing than FAISS at 100M/96-dim, but on a 4-core/3GHz machine HNSW construction is the cost center. Expect roughly 2-6 hours to build a 14.4M-node graph single-pass on 4 cores (HNSW build is O(N log N) distance comps; b1 Hamming is cheap and SIMD-popcount-accelerated, so binary build is materially faster than f32). Must be chunked/off-thread and resumable. Query latency: with a binary HNSW fully RAM-resident, candidate retrieval is sub-millisecond to a…
- native/supply: usearch crate v2.25.3 (2026-05-24), Apache-2.0, maintainer Unum Cloud, ~4.2k GitHub stars. Build: pulls the vendored single-header C++11 core and compiles it during cargo build via `cxx`/`cxx-build` (deps: cxx ^1.0.194, optional numkong>=7.5.0 for SIMD). It does NOT discover system libs via Homebrew/apt/pkg-config and does NOT download/clone C/C++ in build.rs — the C++ source ships inside the published crate. This MATCHES PathKeep's preferred pattern (Rust crate that vendors/compiles its C/C++ b…
- incr: Strong fit for PathKeep's incremental-import model. usearch supports in-RAM add and on-the-fly remove/relabel, so new visit rows append to the graph without a full rebuild, and deletes are tombstoned. Because the binary index is only ~3-4 GB it can be held in RAM during a session, mutated, and re-serialized to the sidecar atomically. Staleness: the…
- pros: Vendored-C++-in-crate build matches PathKeep's preferred native-dep pattern exactly (no system-lib discovery, no build.rs downloads) — same trust class as rusqlite bundled-sqlcipher | Binary (b1x8) quantization in-index makes a 14.4M×1024 HNSW fit in ~3-4 GB RAM, the ONLY config that satisfies the 8 GB envelope with headroom | uint40 neighbor IDs cut graph overhead ~37.5% vs 8-byte IDs; SIMD popcount makes binary build/search fast on CPU with no GPU | In-RAM add/remove/relabel supports incremental import and deletes without full rebuilds; serialize to a rebuildable sidecar
- cons: 4.2k stars is below PathKeep's ~6k trust gate and Unum is a small single org — requires a written risk assessment before adoption | NOT a two-stage engine: quantized-coarse + exact-rerank must be built by PathKeep around it; one index = one precision, search distance = stored precision | view()/mmap disk-serving mode is a performance trap at 14.4M — random HNSW graph hops over a multi-GB mmapped file cause ~20,000 ns SSD page faults (200x RAM), unacceptable for snappy desktop search; only the in-RAM binary config is viable | Binary quant alone loses recall (vanilla BQ 1-bit trails TurboQuant/PQ by 9-21 pp); rerank with full vectors is mandatory, and those 59 GB of f32 vectors must live on disk
- integration: Pipeline: (1) Embed visit rows with Qwen3-Embedding-0.6B (1024-dim, MRL-truncatable, available as ONNX int8 ~573 MB / GGUF ~378 MB for CPU) on a background worker; default in-app model per spec. (2) Store full-precision (or f16) vectors in a flat rebuildable sidecar keyed by rowid for rerank. (3) Binarize (sign threshold, optionally after MRL truncation to 512) and add to a usearch b1x8 HNSW index (M=16, efConstruction~128), held in RAM (~3-4 GB) and serialized to a sidecar. (4) Query path: embed query -> binarize -> usearch Hamming search efSearch~200 for top ~300 candidate rowids -> fetch th…

### RaBitQ / Extended-RaBitQ as the quantizer for PathKeep's on-disk ANN sidecar — VERDICT: conditional — recommend RaBitQ as the _quantization algorithm_ (it is the correct 2026 SOTA choice for our envelope), but REJECT every current standalone Rust RaBitQ crate on supply-chain grounds. Adopt RaBitQ by vendoring a small, audited, repo-owned pure-Rust implementation of the core (1-bit code + corrective factors + asymmetric query path), built off the Apache-2.0 official spec, paired with our own IVF. Treat the third-party crates only as reference implementations to validate against.

- mem@scale: This is where RaBitQ shines for our 8GB box. At 14.4M × 1024-dim (Qwen3-Embedding-0.6B default): raw f32 = 14.4M×1024×4 = 59.0 GB — impossible in RAM, and the whole reason vectors live in a disk sidecar, not canonical SQLite. RaBitQ 1-bit codes = 1024 bits = 128 B/vec → 1.84 GB. With corrective factors (Elastic: 136 B for a 1024-dim vec) → ~1.96 GB resident. Add IVF centroid assignments/list structure (~4-8 B/vec) → ~2.0-2.1 GB. That FITS in the RAM-resident hot set on an 8GB machine alongside the app and SQLCipher page cache. The full-precision f32 (59 GB) stays on disk (the sidecar) and is touched only for reranking: ~100 candidates/query × 4 KB = ~400 KB random reads/query — fine on SSD, …
- cpu: All numbers are CPU-only, no GPU. RaBitQ 1-bit scan is ~100x faster than f32 distance (VectorChord, 2024-12) because it's SIMD popcount over packed bits. Closest envelope-matched primary benchmark: VectorChord IVF+RaBitQ on LAION-100M (768-dim) on AWS i4i.xlarge = 4 vCPU / 32GB / NVMe (close to our 4-core but more RAM) hit 16.2 QPS @0.95 recall@10 single-thread, scaling ~linearly to 8 threads (2024-12). At our 14.4M (1/7th the vectors) on 4 cores, expect tens-to-low-hundreds QPS @~95% recall@10 …
- native/supply: RaBitQ is just arithmetic (rotation, sign, popcount, dot products) — it needs NO C/C++ native dependency at all; a pure-Rust implementation with std::simd or feature-gated intrinsics is sufficient. That is a major supply-chain WIN vs faiss. Survey of what exists (all checked 2026-06 via crates.io + GitHub APIs): (1) Official VectorDB-NTU/RaBitQ-Library — C++, Apache-2.0, 227 stars, active (pushed 2026-06-09), Python bindings, NO Rust binding, CMake build → would need a banned ad-hoc binding. (2)…
- incr: RaBitQ codes are computed per-vector and independent, so INSERT is trivial: rotate + quantize + append code + assign to nearest IVF centroid. No global retrain needed for inserts. DELETE = tombstone the code + skip at rerank (standard). The weak spot is IVF centroid DRIFT: after large bulk changes the k-means centroids get stale and recall degrades…
- pros: Correct 2026 SOTA quantizer: unbiased estimator with proven asymptotically-optimal error bound (Alon-Klartag), defended by the author vs TurboQuant (DEV.to 2026-03-31); production-validated in Milvus 2.6, Faiss, Elasticsearch, VSAG, VectorC… | Solves our core constraint: 14.4M×1024 drops from 59 GB raw to ~2 GB resident 1-bit codes — fits an 8GB box; MRL truncation gives a tunable RAM/recall dial (512-dim ~0.9 GB, 256-dim ~0.46 GB) | PURE arithmetic — needs NO native C/C++ dependency, so a pure-Rust impl sidesteps the entire faiss/BLAS/vcpkg supply-chain problem and cross-compiles to Apple Silicon cleanly | 1-bit SIMD scan ~100x faster than f32 distance; with IVF, only nprobe lists scanned → tens-to-hundreds QPS @~95% recall on 4 cores
- cons: NO standalone Rust crate clears the supply-chain gate: official lib is C++ (227 stars, would need a banned binding); pure-Rust crates are 11/17/3/0 stars, well under 6k, several solo/young/AGPL; rabitq-rs has documented ARM64 distance-calc … | Reranking pages full-precision vectors from disk (~100 reads/query). Fine on SSD; a documented failure mode on HDD ('untenable' — Elastic 2024-10). 59 GB rerank store at 1024-dim f32 is large disk (mitigate with MRL + int8) | IVF centroids drift after large mutations → periodic background re-cluster needed to hold recall | Recall is probabilistic and tuning-sensitive (nprobe, rerank-K, MRL dim); needs an offline recall-measurement harness to set defaults per the data
- integration: 1) Embedding: Qwen3-Embedding-0.6B (default, 1024-dim, MRL) run locally off-thread; user-configurable provider; AI stays disabled-by-default + consent-gated. 2) Build sidecar (background, chunked, resumable): stream rows from canonical SQLCipher → embed → optionally MRL-truncate (512 default dial) → random-rotate → 1-bit RaBitQ encode + corrective factors → assign to IVF list (hierarchical k-means, ~sqrt(N)≈3800 lists for 14.4M) → append code; persist raw/int8 vectors to a separate rerank file. 3) Query: embed query → pick nprobe lists → SIMD scan 1-bit codes for cheap estimates → take top ~10…

### Binary-quantization oversample-and-rescore (disk rerank from mmap'd full/int8-vector sidecar) — VERDICT: recommend

- mem@scale: At 14.4M x 1024-dim. Raw f32 = 14.4e6 x 1024 x 4 = ~59 GB (cannot be resident). Stage-1 resident (the only must-be-in-RAM part): 1-bit codes = 14.4e6 x 128 B = ~1.84 GB; RaBitQ correction floats (2 x f32 = 8 B/vec) add ~0.12 GB -> ~2.0 GB resident, leaving ample room under 8 GB for the app, FTS5, and OS. If a navigable graph (HNSW) sits over the codes, add ~M x 8 B neighbor links; at M=16 that's ~14.4e6 x 16 x 8 B = ~1.8 GB more (~3.8 GB total) — still fits, or push the graph to disk (IVF avoids the graph entirely). Disk: int8 rescore sidecar = 14.4e6 x 1024 B = ~14.7 GB; f32 rescore sidecar = ~59 GB (avoid). Reference check: sbert.net measured 41M -> binary 5.2 GB RAM / int8 47.5 GB disk; l…
- cpu: Build (one-shot 14.4M import): the dominant cost is NOT the index but EMBEDDING GENERATION. Qwen3-Embedding-0.6B runs ~6,878 tok/s on CPU via llama.cpp (~5x slower than peer embedders) = roughly 10-50 embeddings/s depending on chunk length (llama.cpp issue #19933, 2026-02). At ~30 emb/s, 14.4M docs ~= 130+ CPU-hours single-threaded; this must be chunked, resumable, backgrounded, and is the real engineering problem, dwarfing index build. Quantizing+inserting 14.4M codes is comparatively minutes-t…
- native/supply: Pattern itself is algorithmic (no native dep). The native dep is whichever ANN engine implements it. Three options against PathKeep's native-dependency-management.md (path #1 = bundled Rust crate, preferred; #2 = vcpkg; banned = external protoc/brew/clone). (A) usearch (unum-cloud) — v2.25.3 (2026-05-24), Apache-2.0, ~4.2k stars, active. Rust crate on crates.io; ~51% C++ compiled during cargo build via build.rs+cc (bundled, no external CMake/protoc) -> fits path #1 cleanly. Supports b1 (binary) …
- incr: Inserts: append the 1-bit code (and RaBitQ correction floats) to the coarse index and append the int8 vector to the sidecar — both O(1) appends per row; if using HNSW-over-codes, insertion is the usual graph link cost. New rows are immediately searchable. Deletes: ANN graphs/IVF generally tombstone rather than physically remove; usearch supports re…
- pros: Memory math fits 8GB with huge headroom. At PathKeep's 14.4M x 1024-dim: 1-bit coarse index = 14.4M x 128 B ~= 1.84 GB resident (RaBitQ adds ~8 B/vec correction = ~0.12 GB). This is the only stage that must be in RAM. Compare raw f32 = ~59 … | Recall is recoverable to production quality. Binary+rescore retains '~96% of total retrieval performance' at 32x memory reduction (sbert.net). RaBitQ hits >95% recall@k after reranking ~100 candidates on >1M datasets (Elasticsearch RaBitQ e… | Stage-1 search is CPU-trivial and never blocks the UI. Hamming = XOR+popcount, SIMD-friendly, no GPU; this is what lets '40M docs searched in <200ms on CPU, 8GB RAM, 45GB disk' (Coding Nexus, 2026-01) and 'LSM-VEC 94% recall @ 6.2ms' (arXiv… | Sidecar architecture matches PathKeep's storage contract exactly: coarse codes + int8 vectors + graph are all REBUILDABLE derived state in separate files; canonical SQLCipher DB stays clean of vector payloads, as required.
- cons: The skeptic's headline risk: recall is MODEL-DEPENDENT and the PathKeep default model is a relatively low dim. Qwen3-Embedding-0.6B is 1024-dim max with MRL down to 32. Binary quantization 'works best on isotropic, well-trained, high-dim mo… | Disk rescore has random-read amplification. Each query does up to ~(4x k) random reads into the int8 sidecar (~1 KB each at 1024-dim). At top_k=20 x 4 = 80 reads x ~50-100us NVMe = single-digit ms when cold; fine. But it is genuinely random… | Pure-Rust RaBitQ crates are immature/low-trust today. lqhl/rabitq-rs is v0.9.0 (2026-02) but only 17 stars and documents 'ARM64 known correctness issues on Apple Silicon' — a hard blocker for a cross-platform desktop app. The reference RaBi… | Naive (non-rotated) binary is a trap that looks fine on toy data and fails on real data (GIST 35% recall@100). A team could ship it and only discover the recall hole at scale.
- integration: 1) Add usearch Rust crate (bundled C++, path #1) behind a thin internal trait `VectorIndex` so the engine is swappable (usearch now; pure-Rust hnsw_rs later if supply-chain prefers). Write the required short risk assessment (4.2k stars, Apache-2.0, reputable org, bundled build). 2) Sidecar files under the existing var/-style derived-state dir, separate from canonical.db: `vectors.bin` (mmap'd int8, fixed-stride row = 1024 bytes, indexed by row-id), `coarse.usearch` (1-bit/RaBitQ-rotated codes + graph). Both rebuildable; never written into SQLCipher. 3) Embedding worker: read un-embedded rows f…

### hnsw_rs (hnswlib-rs by jean-pierreBoth) — pure-Rust HNSW plan-B — VERDICT: conditional

- mem@scale: Concrete math at N=14.4M (verified by calculation). RAW DATA (no quant, crate's only mode): 1024d f32 = 59.0GB, 512d = 29.5GB, 256d = 14.7GB — all far exceed 8GB, so data MUST be mmap'd from disk (DataMap). GRAPH (always RAM-resident, the binding constraint): with neighbors/node ≈ M0+M and 4-byte ids — M=16 → ~2.76GB, M=24 → ~4.15GB, M=32 → ~5.53GB, M=48 → ~8.29GB. (Note: if the crate uses usize/8-byte ids internally these double to 5.5-16.6GB, which would be fatal — this needs source verification before adoption.) PRACTICAL VERDICT ON 8GB: only survivable at low M (16) AND aggressive MRL dim reduction to keep the mmap'd vector file's hot working set small. Best realistic config: MRL-256 dim…
- cpu: Algorithm is CPU-friendly and SIMD-accelerated. Reference points (from README/benchmarks, not at our scale): SIFT1M (128d) ~8-15k queries/s at 0.99+ recall single-machine; BIGANN-10M recall 0.92-0.99. Extrapolated to 14.4M at 512-1024d: per-query latency would be dominated by disk page-faults into the mmap'd f32 vector file during graph traversal (every neighbor distance needs the full vector), so realistic query latency is bounded by I/O, not CPU — single-digit to tens of ms on NVMe IF the grap…
- native/supply: BEST-IN-CLASS for the supply-chain rules, and the single strongest reason to keep it as plan-B. Language/runtime: 100% Rust. Maintained crate: yes, `hnsw_rs` on crates.io, v0.3.4 (2026-02-28), Rust 2024. Native deps: NONE beyond ordinary Rust crates — it does NOT link any C/C++ system library, needs no BLAS/MKL/OpenBLAS, no vcpkg, no cmake, no build.rs that fetches/clones/compiles external source. Distances via `anndists`; optional SIMD via `simdeez_f` (x86_64) or `stdsimd` (nightly — avoid, kee…
- incr: WEAK — the main functional gap. Insert: supported (multithreaded). Update: not supported (re-insert creates a duplicate; no key-based replace). Delete: NOT supported at all — there is no tombstone or removal API. Consequence for PathKeep: any pruning/redaction/dedup of canonical visits cannot be reflected incrementally; the sidecar drifts until a F…
- pros: Pure Rust, clean vendored build: pulls anndists/rayon/serde/parking_lot/mmap-rs, no C/C++ link, no BLAS/MKL/OpenBLAS, no external system lib, no vcpkg — perfectly satisfies the supply-chain 'prefer crate, native deps build in cargo' rule wi… | Actively maintained: v0.3.4 released 2026-02-28, on Rust 2024 edition, ~100k downloads/month — not abandoned (contrast the Rust DiskANN ports which are research-grade/archived). | Dual MIT/Apache-2.0 license — no licensing risk. | Good recall at small/medium scale: README cites BIGANN-10M recall 0.92-0.99, SIFT1M 0.99+ recall at 8-15k qps, Fashion-MNIST 0.977 recall @ 62k req/s — the algorithm itself is sound.
- cons: GRAPH IS RAM-RESIDENT, NO DISK-GRAPH MODE. At 14.4M the graph alone is ~2.76GB (M=16, u32 ids) up to ~8.29GB (M=48). This is the disqualifier for the primary slot: it consumes most/all of an 8GB budget before vectors, OS, Tauri webview, and… | NO QUANTIZATION (no PQ/scalar/binary). Vectors are stored at input precision. At 1024d f32 that's 59GB of data; PathKeep would have to build its own int8/binary compression + rerank pipeline on top, which the crate does not help with. | NO DELETE / NO UPDATE. The canonical archive is mutable (visits can be pruned/redacted); removing a vector requires a full index rebuild. For a 14.4M sidecar that is a multi-hour rebuild, not an incremental op. | 242 GitHub stars — fails the >6k trust gate, so adopting it REQUIRES a written risk assessment (single-maintainer project, modest adoption).
- integration: Plan-B wiring (only if usearch is rejected): (1) Default embedding = Qwen3-Embedding-0.6B; exploit its Matryoshka (MRL 32-1024, sweet spot ~512, usable down to 256) to TRUNCATE to 256-512 dims at embed time — this is the lever that makes hnsw_rs fit 8GB, since the crate can't quantize. (2) Self-apply int8 scalar quantization on the truncated vectors before insert (store as byte vectors with cosine/L2; ~4x smaller) since the crate won't do it — accept that there is no native full-precision rerank, OR keep a separate on-disk f32 blob and do a manual top-k rerank in PathKeep code after the ANN se…

## CRITIC FINAL REC: Build a purpose-built, pure-Rust IVF + RaBitQ + disk-rerank sidecar — NOT usearch-HNSW, and NOT a disk-graph (DiskANN/Vamana) — sized in tiers, with embedding generation treated as the primary cost.

CONCRETE STACK:

1. DEFAULT = no ANN at all below a threshold (~1M vectors): int8/uint8 FLAT SIMD scan over RAM-resident codes (1M x 1024 B = ~1 GB). Exact recall, zero build, zero corruption risk. This covers most users for years and honors 'AI is optional/additive.'
2. ABOVE the threshold: IVF (inverted-file) routing. Builds in minutes on 4 cores (vs HNSW hours-days); a query reads a few WHOLE posting lists = sequential-ish, batchable SSD reads instead of dozens of dependent QD1 random hops; trivial incremental append + minutes-scale periodic re-cluster. Keep the centroid layer tiny in RAM (tens of MB, per arXiv:2602.21514 MemGraph 0.1%-vertices / 30-50 MB pattern).
3. QUANTIZATION = RaBitQ (1-bit + 2 corrective scalars), ported to portable Rust SIMD. Default to Matryoshka-512 codes (~0.9-1 GB RAM for 14.4M) for the 8 GB box; expose 1024-dim as a 'high-recall' opt-in. RaBitQ over PQ because: no k-means codebook to train (cheap incremental insert), theoretical O(1/sqrt(D)) error bound, fast estimated distances.
4. RERANK tier = Qwen3-0.6B uint8 ONNX 1024-dim vectors on disk (14.75 GB, ~1% NDCG loss) — NOT f32 (59 GB). Oversample 2-4x candidates from IVF/RaBitQ, mmap-read their uint8 vectors, rescore, return top-K. 4x less rerank I/O than f32, materially friendlier to the consumer SSD and tiny page cache.
5. HOT/COLD split for the 60-year horizon: keep recent (~1-3M) vectors' codes hot in RAM, long tail cold on disk; most history queries are recent.

NATIVE-DEP / SUPPLY-CHAIN: this stack is pure Rust (RaBitQ math + IVF + mmap via memmap2) with NO new C/C++ native dependency beyond the already-required SQLCipher/OpenSSL/ONNX-Runtime toolchain, which is the cleanest possible supply-chain outcome and beats usearch (4.2k stars, needs a risk note) and any <=10-star DiskANN port. ONNX Runtime (via the `ort` crate, vendored) covers Qwen3-0.6B INT8/uint8 inference. If a hand-rolled IVF is judged too much work for v1, hnsw_rs (pure Rust, ~242 stars, risk-note) is the plan-B router — but ONLY with RaBitQ codes + uint8 disk rerank layered on, and accepting its hours-scale CPU build.

NON-NEGOTIABLES FOR THE ENVELOPE: (a) embedding the archive is a resumable, throttled, OFF-thread background job, never a blocking precondition — the app stays fully usable on FTS5 + deterministic intelligence while 0% embedded; (b) all rerank/disk reads are off the UI thread, chunked, cancellable, with skeletons; (c) every AI answer cites the real history rows it reranked (transparency); (d) measure on actual 4-core/8GB/consumer-SSD hardware before claiming 'fast' — server SSD/Xeon benchmarks do not transfer. Reconsider a disk-graph index ONLY if a >6k-star, reputable-maintainer Rust DiskANN with built-in quantization and cheap deletes appears AND it beats IVF on cold-cache QD1 latency on the target box.

## CRITIC scale_failure: Concrete breakage on a 4-core/3GHz / 8GB / no-GPU / consumer-SSD desktop at 14.4M x 1024-dim Qwen3-0.6B vectors:

(1) THE EMBEDDING WALL IS THE REAL FIRST FAILURE, AND THE RESEARCH BARELY QUANTIFIES IT. Before any index exists you must run 14.4M documents through a 0.6B-param model. The one real CPU data point found is ~30ms/embedding for a _minimal_ request on an Intel i9-13900HK (8 P-cores + 8 E-cores, far stronger than the 4-core target) per the Qwen HF discussion (huggingface.co/Qwen/Qwen3-Embedding-0.6B/discussions/48). The 44K tok/s figure (github.com/jakedahn/qwen3-embeddings-mlx) is Apple-Silicon MLX (GPU/ANE), NOT a 4-core CPU, and must not be used for envelope math. Realistic 4-core CPU throughput for a 0.6B encoder with real history-page text is on the order of tens of docs/sec even with INT8 ONNX (2-4x over f32, per electroglyph/janni-t model cards). At, say, 50 docs/sec, 14.…
