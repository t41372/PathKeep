# cfg_block

Vendored `cfg_block` 0.1.1 with SPDX metadata added so workspace license checks stay deterministic.

The crate source remains functionally identical to the upstream release used by the dependency graph.
