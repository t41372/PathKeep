/**
 * Barrel export for split Core Intelligence front-end API modules.
 *
 * Why this file exists:
 * - M10 decomposes the old API mega-file without forcing callers to change
 *   their import paths.
 */

export * from './api/index'
